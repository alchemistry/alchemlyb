---
version: 2

sphinx:
  configuration: docs/conf.py

formats:
  - pdf

build:
  os: "ubuntu-24.04"
  tools:
    python: "mambaforge-23.11"

conda:
    environment: docs/requirements.yaml

python:
  install:
    - method: pip
      path: .
